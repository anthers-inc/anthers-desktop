{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "Base capability for the Anthers Studio window: core IPC (the sign-in commands), event listening (the shell emits `desktop-auth-code` when the browser handoff returns), and opening the authorize page in the SYSTEM browser. The http plugin is used from Rust only — the webview gets no direct HTTP scope.",
	"windows": ["main"],
	"permissions": ["core:default", "core:event:default", "opener:default"]
}
